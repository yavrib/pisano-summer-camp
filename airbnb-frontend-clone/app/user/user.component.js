import userTemplate from 'text!./user.html';
import UserController from './user.controller';

const UserComponent = {
  template: userTemplate,
  controller: UserController
};

export default UserComponent;
